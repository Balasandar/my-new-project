name: Deploy with Nginx

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy index.html to Nginx
        run: |
          scp -r ./index.html usr/share/nginx/html/

